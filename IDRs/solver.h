#ifndef __solver_h__
#define __solver_h__

typedef void (*op_t)(int, complex*, complex*); //define type for linear operator
typedef void (*rop_t)(int, double*, double*); //define type for linear operator

#endif
